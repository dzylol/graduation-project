# Bi-Mamba-Chem 分子性质预测实验报告

## 摘要

本报告系统性地介绍了基于双向状态空间模型（Bi-Mamba）的分子性质预测方法。我们将该方法与基于消息传递神经网络（MPNN）的经典方法进行了对比，并在 HIV、Lipophilicity、ZINC250K 等多个分子数据集上进行了实验评估。实验结果表明，Bi-Mamba（max pooling）在 HIV 分类任务上达到了与 D-MPNN 可比的性能水平（AUC 0.787 vs 0.786），但在回归任务上的表现仍有提升空间。此外，我们对双向机制、池化策略等关键设计选择进行了消融实验分析，为后续研究提供了重要参考。

**关键词**：双向状态空间模型，分子性质预测，O(N) 线性复杂度，深度学习，RDKit

---

## 1. 实验背景与目标

### 1.1 研究背景

分子性质预测是药物发现和材料科学中的核心任务之一。准确预测分子的物理化学性质、生物活性以及毒理学特性，能够显著加速新药研发和功能材料的设计过程。近年来，基于深度学习的方法，尤其是图神经网络（GNN）和 Transformer 架构，在分子性质预测领域取得了显著进展。

然而，现有的主流方法存在一个根本性的计算瓶颈：Transformer 架构的自注意力机制具有 O(N²) 的时间和空间复杂度，其中 N 表示输入序列的长度。当处理长序列分子（如蛋白质或聚合物）时，这一复杂度会成为限制模型效率和可扩展性的主要因素。

### 1.2 研究目标

本研究旨在探索一种名为 Bi-Mamba 的双向状态空间模型（State Space Model，SSM）在分子性质预测任务中的应用。该模型的核心优势在于其 O(N) 的线性复杂度，理论上能够高效处理任意长度的分子序列。具体而言，我们的研究目标包括：

1. 验证 Bi-Mamba 模型在分子性质预测任务上的有效性
2. 通过消融实验分析关键设计选择对模型性能的影响
3. 与现有的主流方法（如 D-MPNN、MPNN）进行性能对比
4. 评估模型的计算效率，验证其在长序列上的理论优势

### 1.3 硬件环境

所有实验均在 NVIDIA RTX 5060 Ti 16GB 显卡上完成。该显卡基于 Ada Lovelace 架构，拥有 16GB GDDR6X 显存，为大规模分子数据集的训练提供了充足的计算资源。

---

## 2. v2 优化策略

### 2.1 优化背景

在 v1 版本的基础上，我们进行了系统性的优化调整，旨在提升模型的训练稳定性、降低显存占用，并进一步逼近 Mamba 官方推荐的最佳实践。v2 优化主要涉及以下几个关键维度：

### 2.2 优化项详解

**表 1：v2 优化项对比**

| 优化项 | 原始值 | 优化后 | 效果说明 |
|--------|--------|--------|----------|
| d_state | 64 | 16 | 状态维度减半，显存节省约 50%，同时保留了足够的状态表达能力 |
| AdamW β | (0.9, 0.999) | (0.9, 0.95) | 采用 Mamba 官方推荐的二阶矩估计参数，有助于提升收敛稳定性 |
| weight_decay | 1e-5 | 0.1 | 显著增强的正则化强度，防止模型过拟合 |
| 学习率调度 | Linear | CosineAnnealing | 余弦退火调度使学习率在训练后期平滑下降，收敛更稳定 |
| 混合精度 | fp16 | bfloat16 | bfloat16 具有更宽的动态范围，避免精度溢出问题 |
| 早停机制 | 无 | patience=15 | 监控验证集损失，连续 15 轮无改善则停止训练，防止过拟合 |

这些优化项的调整基于以下考虑：d_state 的缩减直接降低了 SSM 的状态空间维度，从而减少了需要存储和更新的参数量；AdamW 参数的调整遵循了 Mamba 论文中的经验建议；weight_decay 的增加提供了更强的正则化效果；CosineAnnealing 调度相比线性衰减能够更好地平衡探索与利用；而 bfloat16 格式则在保证数值稳定性的同时保持了较高的计算效率。

---

## 3. 数据集与划分策略

### 3.1 数据集概述

我们选择了多个具有代表性的分子性质预测数据集进行实验，涵盖了分类任务和回归任务两种类型。所有数据集均来自公开的 MoleculeNet 基准。

**表 2：实验数据集统计**

| 数据集 | 任务类型 | 分子数量 | 特点与应用场景 |
|--------|----------|----------|----------------|
| HIV | 二分类 | 41,127 | 抗 HIV 病毒活性筛选，药物发现早期评估 |
| ZINC250K | 回归 | 249,455 | 药物候选分子的亲脂性预测 |
| Lipophilicity | 回归 | 4,200 | 候选分子的脂水分配系数 |
| mpro | 回归 | 40 | 主蛋白酶活性数据，仅供参考 |

### 3.2 数据划分

为了确保实验结果的可复现性并避免数据泄露，我们对所有数据集采用了 Scaffold Split 划分策略。该策略基于分子的骨架结构（scaffold）进行划分，能够更真实地模拟新药发现场景中模型对未见过的化学骨架的泛化能力。划分比例为训练集 80%、验证集 10%、测试集 10%，随机种子固定为 42 以确保可复现性。

需要特别说明的是，mpro 数据集仅包含 40 个分子，样本量过小，无法进行有意义的统计推断其实验结果仅作为参考，不纳入正式的性能对比分析。

### 3.3 Scaffold Split 的合理性

Scaffold Split 相比随机划分更具挑战性，因为模型需要在测试集上预测与训练集化学骨架不同的分子。这种划分方式更接近实际应用场景：新药发现往往需要评估与已知化合物骨架差异较大的新颖分子。因此，Scaffold Split 下取得的性能通常被视为更有价值的参考指标。

---

## 4. 训练配置

### 4.1 超参数设置

我们采用了一致的训练配置以确保实验的公平性和可比性。以下是主要的训练超参数：

**表 3：训练超参数配置**

| 超参数 | 值 | 说明 |
|--------|-----|------|
| 模型类型 | mamba_ssm | 使用 mamba_ssm 官方实现 |
| d_model | 256 | 模型的隐藏层维度 |
| d_mamba | 256 | Mamba 层的内部维度 |
| d_state | 16 | SSM 的状态维度（v2 优化） |
| n_layers | 4 | 模型层数 |
| 池化策略 | mean/max/cls/uni | 四种池化方式对比 |
| 批次大小 | 32 | 每批次处理的分子数量 |
| 初始学习率 | 1e-4 | 使用余弦退火调度 |
| weight_decay | 0.1 | AdamW 正则化强度 |
| 早停耐心值 | 15 | 验证集无改善的最大轮数 |
| 最大训练轮数 | 100 | 包含早停机制 |

### 4.2 训练策略

模型训练采用交叉熵损失（分类任务）或均方误差损失（回归任务）作为目标函数，使用 AdamW 优化器进行参数更新。梯度裁剪阈值设置为 1.0，以防止梯度爆炸。我们采用线性warmup策略在前 5 个 epoch 逐步增加学习率，此后使用余弦退火调度逐步降低学习率。

早停机制基于验证集损失实现：若连续 15 个 epoch 验证集损失未下降，则终止训练并恢复最佳模型权重。

---

## 5. 主实验结果

### 5.1 HIV 分类任务

HIV 数据集是一个大规模二分类基准，旨在预测分子是否具有抗 HIV 病毒活性。我们对比了四种不同池化策略的 Bi-Mamba 模型。

**表 4：HIV 数据集分类性能对比**

| 模型配置 | AUC | 训练轮数 | 备注 |
|----------|-----|----------|------|
| Bi-Mamba (mean) | 0.756 | 15 | 早停 |
| Bi-Mamba (max) | **0.787** | 15 | 早停 |
| Bi-Mamba (cls) | 0.498 | - | 训练失败，接近随机猜测 |
| Bi-Mamba (uni) | 0.737 | 15 | 早停 |

**分析**：Max pooling 配置的 Bi-Mamba 取得了最佳性能（AUC = 0.787），这表明在 HIV 分类任务中，模型能够从序列中提取最具区分性的特征。Mean pooling 次之，Uni-directional 配置略低于 mean pooling。有趣的是，CLS pooling 配置表现极差，AUC 仅 0.498，接近随机猜测水平（0.5）。可能的原因之一是：在 HIV 数据集中，CLS token 无法有效聚合分子全局信息，或者其优化目标与分类边界无关。所有早停的模型均在第 15 轮停止训练，说明模型在早期即出现了过拟合迹象。

### 5.2 Lipophilicity 回归任务

Lipophilicity（亲脂性）是药物化学中的关键性质，直接影响分子的吸收、分布和代谢特性。

**表 5：Lipophilicity 数据集回归性能对比**

| 模型配置 | RMSE | 训练轮数 | 备注 |
|----------|------|----------|------|
| Bi-Mamba (mean) | 1.38 | 15 | 早停 |
| Bi-Mamba (max) | 1.48 | 15 | 早停 |
| Bi-Mamba (cls) | **1.19** | 100 | 完整训练 |
| Bi-Mamba (uni) | 1.38 | 15 | 早停 |

**分析**：与 HIV 分类任务形成鲜明对比的是，CLS pooling 在 Lipophilicity 回归任务上取得了最佳性能（RMSE = 1.19），且是唯一完成全部 100 轮训练的配置。这表明 CLS token 作为可学习的全局表示载体，在回归任务中能够有效聚合分子特征。Mean 和 Uni 配置性能相同（RMSE = 1.38），而 Max pooling 反而表现最差（RMSE = 1.48）。这一结果提示我们，池化策略的选择应基于具体任务特性而非通用经验。

### 5.3 ZINC250K 回归任务

ZINC250K 是规模最大的药物类似物数据集之一，用于预测分子的亲脂性等理化性质。

**表 6：ZINC250K 数据集性能**

| 模型配置 | RMSE | 训练轮数 | 备注 |
|----------|------|----------|------|
| Bi-Mamba | 1.42 | 15 | 早停 |

由于时间资源限制，我们仅在 ZINC250K 上测试了默认配置（mean pooling）的性能。RMSE 为 1.42，早停在第 15 轮触发。考虑到该数据集包含近 25 万个分子，模型的收敛速度较快，但仍有进一步优化的空间。

---

## 6. 与 Baseline 方法对比

### 6.1 对比方法说明

我们将 Bi-Mamba 模型与当前分子性质预测领域的主流方法进行了对比。PharmHGT（2023）是基于异构图 Transformer 的分子表示学习方法，在多个 MoleculeNet 基准上取得了领先性能。D-MPNN（Directed Message Passing Neural Network）是一种经典的分子图神经网络，通过定向消息传递机制捕捉分子图的结构信息。MPNN 则是其简化版本，删除了方向向量但保留了核心的消息传递机制。

### 6.2 性能对比结果

**表 7：Bi-Mamba 与 Baseline 方法性能对比（Scaffold Split）**

| 模型 | HIV AUC | Lipophilicity RMSE | ESOL RMSE | FreeSolv RMSE |
|------|---------|-------------------|-----------|---------------|
| **Bi-Mamba (max)** | **0.787** | - | - | - |
| Bi-Mamba (cls) | 0.498 | 1.19 | - | - |
| D-MPNN | 0.786 | 0.653 | 1.167 | 2.185 |
| Bi-Mamba (mean) | 0.756 | 1.38 | 2.036 | 3.834 |
| MPNN | 0.741 | 0.672 | 1.167 | 2.185 |

**注**：Bi-Mamba 在 HIV 分类任务上与 D-MPNN 性能相当，但在所有回归任务上均明显逊于 D-MPNN/MPNN。回归任务中，SSM 的离散状态空间表示可能引入近似误差，且小数据集上容易过拟合。

**表 8：效率对比（RTX 5060 Ti，完整 O(N) 趋势）**

| 序列长度 | BiMamba 时间 | BiMamba 显存 | Transformer 时间 | Transformer 显存 | 时间比 (Bi/Trans) | 显存比 (Bi/Trans) |
|----------|-------------|--------------|-----------------|-----------------|-------------------|-------------------|
| 64 tokens | 3.79ms | 336MB | 1.60ms | 65MB | 2.37x | 5.19x |
| 128 tokens | 5.47ms | 358MB | 3.07ms | 105MB | 1.78x | 3.42x |
| 256 tokens | 9.49ms | 405MB | 8.00ms | 233MB | 1.19x | 1.74x |
| 512 tokens | 19.72ms | 503MB | 22.61ms | 681MB | **0.87x** | **0.74x** |

**注**：时间比 < 1 表示 BiMamba 更快，显存比 < 1 表示 BiMamba 更节省显存。实验表明，在 512 tokens 长度时 BiMamba 开始展现出速度优势，且显存占用显著低于 Transformer。

### 6.3 结果分析

在 HIV 分类任务上，Bi-Mamba (max) 与 D-MPNN 性能相当（0.787 vs 0.786）。需要说明的是，由于仅进行了单次实验且差距仅为 0.001，我们不能声称具有统计学意义上的显著差异。这一结果仅表明双向状态空间模型在分子分类任务上具有竞争力。

**回归任务普遍表现不佳**：Bi-Mamba 在 Lipophilicity（RMSE 1.38 vs 0.653）、ESOL（RMSE 2.036 vs 1.167）和 FreeSolv（RMSE 3.834 vs 2.185）三个回归数据集上均明显逊于 D-MPNN/MPNN。这一现象的可能原因包括：

1. **SSM离散状态空间的固有局限**：状态空间模型通过离散化近似连续序列，对连续数值预测引入了一定误差
2. **小数据集过拟合**：ESOL（1,144样本）和 FreeSolv（642样本）规模较小，Bi-Mamba 参数量大（379万）容易过拟合，即使增强正则化效果有限
3. **分类vs回归的任务特性差异**：分类任务的决策边界相对鲁棒，而回归任务需要精确的数值输出，对模型输出层要求更高

**效率分析显示 O(N) 特性的初步验证**：BiMamba 在短序列时比 Transformer 慢，但随着序列长度增加，两者速度差距逐渐缩小并在 512 tokens 时逆转。这与 O(N) vs O(N²) 的复杂度差异相符。更长的序列（如 1024/2048 tokens）因模型 position embedding 限制未能测试，但趋势已初步显现。BiMamba 的双向处理在短序列上带来较大常数因子开销，但该开销不随序列长度增长，符合线性复杂度的预期。

---

## 7. 消融实验分析

### 7.1 d_state 消融实验

我们通过在 ESOL 数据集上改变 `d_state` 参数（16/32/64），研究状态空间维度对模型性能的影响。

**表 9：d_state 消融实验结果（ESOL 数据集）**

| d_state | RMSE | RMSE_orig |
|---------|------|-----------|
| 16 | 2.015 | **1.999** |
| 32 | 2.048 | 2.017 |
| 64 | 2.037 | 2.002 |

实验表明，`d_state=16` 在 ESOL 数据集上取得最佳性能（RMSE_orig=1.999），可能是因为较小的状态空间减少了过拟合风险。较大的 d_state 值（32/64）引入了更多参数，但并未带来性能提升。

### 7.2 双向机制的影响

我们通过对比 Uni-directional 和 Bidirectional（mean pooling）配置，评估了双向机制对模型性能的影响。

**HIV 分类任务**：双向配置（mean）AUC 为 0.756，单向配置为 0.737，提升约 0.019。双向机制允许模型同时从正向和反向两个方向捕捉分子序列中的依赖关系，有助于更全面地理解分子结构。

**Lipophilicity 回归任务**：双向配置（mean）和单向配置的 RMSE 均为 1.38，双向机制未带来明显收益。这表明对于回归任务而言，序列方向性信息的重要性可能较低。

### 7.3 池化策略的影响

池化策略决定了如何将分子序列的token表示聚合为固定维度的分子表示，是影响模型性能的关键因素。

**HIV 分类任务**：Max (0.787) > Mean (0.756) > Uni (0.737) >>> CLS (0.498)。Max pooling 通过选取最显著的特征激活，在分类任务中表现出色。CLS pooling 的失败可能与 HIV 数据集中分子表示的分布特性有关。

**Lipophilicity 回归任务**：CLS (1.19) > Mean (1.38) = Uni (1.38) > Max (1.48)。回归任务与分类任务展现出截然不同的模式，CLS pooling 成为最佳选择。这提示我们，CLS token 作为可学习的全局表示，在需要精确数值预测的场景中更具优势。

### 7.4 CLS Pooling 的特殊性

CLS pooling 在两类任务上表现出了极端的反差：在 HIV 分类上接近随机（AUC = 0.498），在 Lipophilicity 回归上却取得最佳性能（RMSE = 1.19）。

可能的原因之一是：CLS token 的优化目标与具体任务性质高度相关。在分类任务中，CLS token 可能被错误地引导到了与分类边界无关的特征空间区域；而在回归任务中，连续的数值预测目标可能更有利于 CLS token 学习的全局表示的优化。这一现象可能的原因较为复杂，值得进一步深入研究，目前尚无定论。

---

## 8. 结论与建议

### 8.1 主要结论

1. **Bi-Mamba (max pooling) 在 HIV 数据集上与 D-MPNN 达到可比的性能水平**（0.787 vs 0.786），证明了双向状态空间模型在分子分类任务上的竞争力。

2. **回归任务是 Bi-Mamba 的明显短板**：在 Lipophilicity（1.38 vs 0.653）、ESOL（2.036 vs 1.167）和 FreeSolv（3.834 vs 2.185）三个数据集上，Bi-Mamba 均显著落后于 D-MPNN/MPNN，差距从 1.1x 到 1.8x 不等。SSM 的离散状态空间表示可能是主要限制因素。

3. **池化策略的选择高度依赖任务特性**：分类任务推荐 Max pooling，回归任务推荐 CLS pooling，Mean pooling 可作为通用默认选项。

4. **BiMamba 在长序列时展现出效率优势**：在 512 tokens 时 BiMamba 比 Transformer 快 15%（19.72ms vs 22.61ms），且显存节省 26%（503MB vs 681MB）。O(N) 特性在长序列场景下开始显现。

5. **CLS pooling 在 HIV 分类任务上失败**（AUC 0.498），但在 Lipophilicity 回归任务上表现最佳（RMSE 1.19），任务适配性差异显著。

6. **d_state 消融实验表明 d_state=16 为最优配置**：在 ESOL 数据集上，d_state=16 取得最佳性能（RMSE_orig=1.999），且显著节省显存（减少约 50%），是回归任务的推荐配置。

### 8.2 改进建议

针对上述问题，我们提出以下改进方向：

1. **针对回归任务的架构优化**：探索将回归目标进行离散化处理（如分桶回归），或引入辅助损失函数改善数值精度。

2. **降低回归任务过拟合风险**：采用更激进的正则化（更高weight_decay）、更小的模型配置（d_model=128, n_layers=2），或采用预训练-微调策略。

3. **效率优化**：分析并优化双向 SSM 的常数因子开销，可考虑混合架构（短序列用 Transformer，长序列用 SSM）。

4. **更大规模的消融实验**：在更多数据集上验证当前发现的普遍性。

### 8.3 局限性说明

本研究的局限性包括：仅在 5 个数据集上进行了实验；未对 Mamba 层的内部机制进行深入分析；效率对比仅覆盖了 64-512 序列长度范围；回归任务的劣势需要更深入的理论分析。

---

## 参考文献

[1] Gu A, Dao T. Mamba: Linear-Time Sequence Modeling with Selective State Spaces. arXiv:2312.00752, 2023.

[2] Zhou Z, et al. PharmHGT: Heterogeneous Graph Transformer for Molecular Property Prediction. ICML, 2023.

[3] Gilmer J, et al. Neural Message Passing for Quantum Chemistry. ICML, 2017.

[4] Wu Z, et al. MoleculeNet: A Benchmark for Molecular Machine Learning. Chemical Science, 2018.

---

*本报告基于 2026 年 4 月在 RTX 5060 Ti 16GB 显卡上的实验数据生成。*